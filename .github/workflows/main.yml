name: Deploy to Hostinger
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"
      - name: Install dependencies
        run: npm install
        # Check the directory structure to ensure build/ exists
      - name: Build project
        run: npm run build
        #
      - name: 📂 Sync files
        uses: https://github.com/SamKirkland/FTP-Deploy-Action.git
        with:
          server: taytaypalawan.net
          username: u523577370
          password: micto.software.camposano#25
          local-dir: dist/
          server-dir: /public_html/ai-test/
